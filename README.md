# Тестовое задание DiveSea

<!-- - [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Babel](https://babeljs.io/) (or [oxc](https://oxc.rs) when used in [rolldown-vite](https://vite.dev/guide/rolldown)) for Fast   Refresh -->

### После клонирования
```
npm install
```

### Запуск в режиме разработки (доступно на [http://localhost:5173](http://localhost:5173))
```
npm run dev
```

### Сборка и запуск production (доступно на [http://localhost:5173](http://localhost:4173))
```
npm run build
npm run preview
```

### Запуск в контейнере
```

```